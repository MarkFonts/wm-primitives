# Spec-fidelity run — 2026-09-17

Three models, one run each, on commit `e1bfc03`. Given `docs/` and the stub only.
Scored per [rubric.md](../rubric.md); mechanical rows by `score.mjs`, judgement rows by
the session that ran the book (this one). The list at the end is the deliverable.

| | haiku | sonnet | opus |
| --- | --- | --- | --- |
| T1 builds & renders | 2 | 2 | 2 |
| T1 auto | 2 | 2 | 2 (also set `autoValue` so the pill shows) |
| T1 column (R1) | 2 — 331/331/331 | 2 — 331/331/331 | 2 — and raised `--axis-label-w` for the pill, per DIAL §5 |
| T2 mechanism | **0 — edited `shared/src/AxisSlider.css`**, against the task's own rule | 2 — `--accent: var(--signal)` on `:root`, unlayered | 2 — same, plus the `@layer` order statement placed first |
| T2 ink step | 1 — rewrote the primitive's rule with `--ink-quiet` | 1 — judged `--text-muted` already the quiet step, changed nothing | 2 — pinned `--text-muted: rgba(var(--text-rgb), var(--ink-quiet))` by name |
| T3 placement | 2 — beside `a` | 2 — beside `a` | 2 — beside `a`, and on the `reference` prop doc |
| T3 spec | **0 — inserted G27 and renumbered G27–G36**, which would break every test that cites an id | 2 — appended G37 | 2 — appended G37 |
| T3 judgement | 1 — "only when `reference` exists", which makes `100` untypeable on any dial with a stock value | 2 — command only while `draft == null`; digit after the first keystroke; said so | 2 — same rule, plus modifier guard, plus the residual case named |
| **total / 16** | **10** | **15** | **16** |
| tokens · minutes | 113k · 3 | 176k · 5 | 112k · 5 |

**Not shipped:** the `0`-resets-to-reference shortcut was a task, not a request. The two
good implementations are in the run directories if it is ever wanted; the rule they both
derived is now in GESTURES §3 so the next person does not have to derive it.

## Where the docs were silent

Every wrong turn, the sentence that would have prevented it, and where it now lives.

| # | who turned wrong | the missing sentence | put in |
| --- | --- | --- | --- |
| 1 | haiku (T3) | **Gesture ids are permanent. Append; never renumber** — tests cite them. | GESTURES.md, top |
| 2 | haiku (T2) | To theme every active state, set **`--accent`** on your root (`--accent: var(--signal)`). No selector, and nothing under `shared/`. | DIAL.md §6 |
| 3 | opus, sonnet (T2) | The label's `--text-muted` **is** the quiet ink step — the same number as `--ink-quiet`, by design, in one sentence. | DIAL.md §6 |
| 4 | haiku (T2) | The ink syntax, shown: `rgba(var(--text-rgb), var(--ink-quiet))`. | TYPOGRAPHY.md §II |
| 5 | opus (T1) | Measure the rail's widest row **including the `auto` pill** — it adds ~20px. | DIAL.md §5 |
| 6 | sonnet (T1) | A fresh `allowAuto` dial starts at its **`autoValue`**, as `'auto'`; a number is what the user has chosen. | AxisSliderProps JSDoc (`allowAuto`) |
| 7 | all three (T3) | **When a printable key may be a command in the field:** a key that can appear inside a value is bound only while no draft is in progress; a key that cannot (`a`) may be bound always. | GESTURES.md §3 |
| 8 | sonnet, haiku (T3) | Single-key shortcuts live in `onKeyDown`, beside `a`, each with its promise in GESTURES §3. | comment above `onKeyDown` in AxisSlider.tsx |

Eight sentences. Three were asked for by every model or by the strongest one; the rest by
one. All eight are now in the files named. Next run compares against this table.

## What the run says about the docs

The JSDoc and DIAL.md carry a **wired** dial well: three for three on T1 with no help.
The gaps are at the two edges — *theming from outside* (which token, said once) and
*extending from inside* (where, and by what rule). Both are one paragraph each, and both
paragraphs now exist.
